[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "grovers-algorithm"
version = "0.1.0"
description = "Grover's Search Algorithm with Qiskit"

[tool.setuptools.packages.find]
where = ["src"]
